//首頁，再點擊前的狀態
import { useState, useEffect, useRef, useContext } from 'react'

const First = () => {
    // const { nowShow, setNowShow } = useContext(all_status)
    // console.log(nowShow);
    return (
        <div className='first_page'>
            <h1>First Page</h1>
{/* 
            <button onClick={setNowShow(1)}>

            </button> */}
        </div>
    )
}

export default First